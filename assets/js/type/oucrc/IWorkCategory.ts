export default interface IWorkCategory {
  id: string,
  name: string,
  // eslint-disable-next-line camelcase
  name_english: string,
  // eslint-disable-next-line camelcase
  ordering_key: number,
  color: string
}
